#!/bin/sh
set -e

#DEBHELPER#

systemctl disable --now gdm3 || true
systemctl disable --now sddm || true
systemctl disable --now lightdm || true

echo "kodi-rockchip-gbm: installed and enabled; display managers disabled. Maybe reboot to cleanup."

exit 0
